
import { tie } from 'count-me-up/helpers/tie';
import { module, test } from 'qunit';

module('Unit | Helper | tie');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tie([42]);
  assert.ok(result);
});

