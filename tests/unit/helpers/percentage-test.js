
import { percentage } from 'count-me-up/helpers/percentage';
import { module, test } from 'qunit';

module('Unit | Helper | percentage');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = percentage([42]);
  assert.ok(result);
});

